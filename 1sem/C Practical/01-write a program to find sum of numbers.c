// write a program to find sum of numbers
#include<stdio.h>
#include<conio.h>

main ()
{
    int a,b,s;

    printf("Enter the value of a and b :  ");
    scanf("%d%d", &a, &b); // scan value from keyboard
    s = a+b;
    printf("The sum is: %d", s);
    getch()
    return(0);

}
